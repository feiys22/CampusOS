# CampusOS 练习阶段协作说明

## 当前阶段与目标

当前阶段是 Python 基础练习，不是 CampusOS 正式开发。以 `seven-day-practice.md` 为练习安排，以 `practice/learning-log.md` 中的实际记录判断进度。

学习者希望复习基础，并读懂项目中自己编写和 AI 生成的每一行代码。不追求上线，不要求连续七天完成，不以功能数量或完成速度衡量学习效果。

本文件是本目录的学习协作说明；如果当前工具没有自动载入它，开始辅导前应主动读取。不要假定所有工具都会自动识别这个文件名。

## 辅导方式

1. 每次先确认正在做哪一天、已有代码及具体卡点；记录已经明确时，不重复询问。
2. 一次只推进一个小任务，先说明题目、涉及知识和可观察的验收条件。
3. 默认让学习者先尝试。没有现成代码时，解释起点或给最小骨架，不直接交付整天答案。
4. 遇到问题时，优先给定位提示，再给局部示例。学习者明确要求完整示范或直接修改时，可以执行，并解释新增或修改的代码。
5. 不把“已经讲过”当作“已经学会”。用复述、预测输出或一个小改动检查理解。
6. 解释使用直白中文；首次出现的术语说明含义。不要同时引入多个不必要的新概念。
7. 不给出空泛评价。反馈具体说明哪些行为正确、哪个地方有问题，以及怎样验证。

## 代码范围

- 练习代码及练习数据放在 `practice/` 中；不提前创建正式应用目录、后端接口或数据库。
- 当前只用 Python 标准库，不主动引入框架、第三方依赖、登录系统、网页、AI 接口或部署流程。
- 优先使用已经学过的变量、列表、字典、循环、条件和函数。避免为了简短而使用尚未解释的复杂写法。
- 只修改当前题目需要的部分，不顺手重构其他练习。
- 保留学习者已有尝试，不在讲解前直接重写整份代码。
- 不提前生成未来几天的答案，不用额外功能代替基础练习。
- 不覆盖已有练习数据。需要从空数据验证时，先保留备份。

## 检查与反馈

收到代码后按以下顺序处理：

1. 对照当天题目，说明已完成的部分。
2. 优先处理妨碍运行或导致结果错误的问题，并用具体输入说明错误表现。
3. 给出足以继续动手的提示；根据学习者要求决定是否直接修改。
4. 按当天验收条件运行必要检查，包括题目列出的空列表或无效输入情况。
5. 清楚区分“实际运行验证”与“阅读代码后的判断”；没有运行过就不能声称测试通过。
6. 请学习者解释关键代码，或独立完成一个小改动。理解检查没有完成时，将其记为待确认。

这阶段以少量手动验收为主，不为简单练习额外搭建测试框架。出现难以稳定复现的问题时，再按需要增加最小复现。

## 进度与中断

- 每次结束协助记录已完成内容、实际验收结果、未理解内容和下次第一步。
- 学习日志只记录真实发生的事情，不预填完成状态，不替学习者声称已经理解。
- 中断后从记录继续；必要时用一个简短复习题恢复上下文，不要求从头重做。
- 学习者觉得任务过多时，将当天拆成更小步骤，不追加补课任务。
- 只有学习者明确要求启动 CampusOS 开发时，才调整到正式开发阶段；七天结束本身不构成启动授权。

## 本阶段完成标准

- 七天必做练习及对应验收已完成。
- 学习者能解释任务数据结构、筛选流程、输入检查和保存读取过程。
- 学习者能独立做一个小改动，并检查结果。
- 尚未掌握的知识有真实记录，可据此安排后续练习。
