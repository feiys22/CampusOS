# CampusOS 启动前：七天 Python 练习

这七天只在项目目录里练习代码，不启动 CampusOS 正式开发。目标是复习基础，逐步写出一个自己能解释、能修改的小程序。

“七天”指七次练习，不要求连续完成。一天没弄懂，可以拆成几次；中断后从上次记录继续，不需要补进度。

## 怎么使用这份练习

- 每次依次完成：阅读题目 → 自己尝试 → 运行检查 → 解释代码 → 记录问题。
- 卡住时，把自己的代码、完整报错和预期结果发给布布，先获取提示，再尝试修改。
- 不以代码行数或速度衡量进度。当天的验收条件完成后，再进入下一天。
- 必做任务之外没有额外作业。暂时不学框架、数据库、网页、AI 接口或部署。
- 使用 Python 标准库即可，不需要安装第三方库。

## 练习文件约定

练习全部放在 `practice/` 下，避免与后续正式项目混在一起。现在不用一次建齐文件，到哪一天再创建对应文件。

```text
practice/
  day01.py
  day02.py
  day03.py
  day04.py
  day05.py
  day06.py
  day07.py
  tasks.json       # 第六天运行程序时生成
  learning-log.md  # 每次练习结束时更新
```

第一天先在终端运行 `python --version`，确认显示 Python 3 的版本号；如果提示找不到命令，可以尝试 `py --version`。两者都不能运行时，先让布布协助检查环境，不急着安装其他工具。

在项目根目录使用 `python practice/day01.py` 运行第一天的代码。如果本机使用的是 `py`，后续命令也相应使用 `py`。

以下任务名称均为练习用数据，不代表真实作业。暂时不处理截止日期，先把数据和程序流程学清楚。

## 第一天：让程序介绍一项任务

**要掌握：** 变量、字符串、布尔值、`print`、f-string。

### 必做任务

1. 创建 `practice/day01.py`。
2. 使用三个变量，分别保存任务名称“阅读课本”、课程名称“Python 基础”、完成状态 `False`。
3. 输出任务名称、课程名称和完成状态，每项各占一行。
4. 把完成状态改为 `True`，再次运行，观察输出变化。

### 验收条件

- 文件能够从终端运行。
- 修改变量后，输出会随之改变，不需要同时修改打印内容里的任务名称。
- 能说明字符串 `"False"` 和布尔值 `False` 的区别。

### 向布布解释

- 变量名与字符串内容有什么区别？
- 为什么给字符串加引号，而给布尔值不加引号？

## 第二天：用列表和字典保存多项任务

**要掌握：** 字典、键和值、列表、索引、`append`、`len`。

### 必做任务

1. 创建 `practice/day02.py`，把下面三项任务保存到一个列表中，每项任务用一个字典表示。
2. 字典统一使用三个键：`title`、`course`、`completed`。
3. 通过列表索引和字典键，打印第二项任务的名称。
4. 使用 `append` 添加一项自己命名的练习任务，打印任务总数。
5. 修改第一项任务的完成状态。

| title | course | completed |
| --- | --- | --- |
| 阅读课本 | Python 基础 | False |
| 整理笔记 | 数据库基础 | True |
| 完成习题 | Python 基础 | False |

### 验收条件

- 第二项任务的名称输出为“整理笔记”。
- 添加一项任务后，总数为 4。
- 修改第一项状态时，其他任务的状态保持原值。
- `completed` 保存的是布尔值，不是文字形式的 `"True"` 或 `"False"`。

### 向布布解释

- 为什么外层用列表，每项任务用字典？
- 列表的第一个元素为什么通过索引 `0` 访问？

## 第三天：筛选未完成任务

**要掌握：** `for`、`if`、`not`、计数、缩进。

### 必做任务

1. 创建 `practice/day03.py`，重新使用第二天表格中的三项初始任务。
2. 用循环和条件判断，只打印未完成任务的名称及所属课程。
3. 统计未完成任务数量，在最后输出“未完成任务：2 项”。数量必须由程序计算。
4. 当没有未完成任务时，输出“当前没有未完成任务”。

### 验收条件

分别运行以下三组数据，每次记录实际输出：

| 输入情况 | 预期结果 |
| --- | --- |
| 三项初始任务 | 显示“阅读课本”和“完成习题”，数量为 2 |
| 三项全部完成 | 不显示任务明细，数量为 0，显示无未完成任务的提示 |
| 空列表 | 不报错，数量为 0，显示无未完成任务的提示 |

### 向布布解释

- 循环中的变量，每一轮代表什么？
- 统计数量的变量应该在哪里初始化？
- 把最后的汇总打印缩进到循环里面，会有什么变化？先预测，再运行检查。

## 第四天：把一段逻辑写成函数

**要掌握：** `def`、参数、调用、返回值、局部变量。

### 必做任务

1. 创建 `practice/day04.py`。
2. 写一个 `get_pending_tasks(tasks)` 函数：接收任务列表，返回一个只包含未完成任务的新列表。
3. 函数内部不负责打印；在调用函数的位置获取结果，再打印任务和数量。
4. 用两份不同的任务列表分别调用函数，不为第二份数据复制一遍筛选逻辑。

继续使用普通循环和条件判断即可，不要求使用列表推导式。

### 验收条件

- 传入三项初始任务时，返回 2 项。
- 传入全部已完成的任务或空列表时，返回空列表。
- 调用后，原任务列表的长度、内容和完成状态没有改变。
- 能把返回值保存到变量中，再使用 `len` 获取数量。

这里的“新列表”只要求筛选时不改变原数据，暂不学习深拷贝，也不通过结果修改任务字典。

### 向布布解释

- `return` 和 `print` 分别把结果交给谁？
- 如果函数只有打印，没有写 `return`，接收调用结果的变量会得到什么？

## 第五天：接收输入，并处理无效内容

**要掌握：** `input`、`strip`、字符串与整数转换、`try/except ValueError`。

### 必做任务

1. 创建 `practice/day05.py`，用三项初始任务作为起点。
2. 写一个 `add_task(tasks, title, course)` 函数：去掉名称和课程两端的空白，任意一项为空就返回 `False`；有效时追加一项未完成任务，并返回 `True`。
3. 在函数外使用 `input` 读取名称和课程，调用函数，并根据结果打印提示。
4. 给当前任务列表显示从 1 开始的序号，让用户输入一个序号，将对应任务标记为完成。
5. 非整数输入和超出范围的序号都给出提示，不修改任务。输入序号 `0` 也属于无效输入。

今天每项输入读取一次即可；无效后给提示，不要求反复追问，也不做菜单。异常处理只包住需要转换整数的代码，不使用一个大范围的 `except` 把整个程序包起来。

### 验收条件

每组检查都从初始数据重新运行：

| 操作 | 预期结果 |
| --- | --- |
| 添加名称和课程都有内容的任务 | 总数增加 1，新任务未完成 |
| 名称仅为空格，或课程为空 | 添加失败，总数不变 |
| 输入有效任务序号 | 只有对应任务变为已完成 |
| 输入 `abc`、`0`、`-1` 或超过任务总数的序号 | 给出提示，不报错，不修改完成状态 |

### 向布布解释

- `input` 返回什么类型？
- 用户看到的序号与列表索引为什么差 1？
- 为什么不能在检查范围之前直接用输入的序号访问列表？

## 第六天：退出程序后保留数据

**要掌握：** `json`、`with open`、UTF-8、文件路径、`pathlib.Path`。

### 必做任务

1. 创建 `practice/day06.py`。
2. 写 `save_tasks(tasks, path)`：将任务列表保存为 JSON 文件，中文直接可读，并使用缩进排版。
3. 写 `load_tasks(path)`：读取 JSON 并返回任务列表；文件不存在时返回空列表。
4. 文件固定保存到当前脚本旁边的 `tasks.json`，用 `Path(__file__).resolve().parent` 确定脚本目录，并请布布解释这条表达式。
5. 第一次运行时，在文件不存在的情况下创建练习数据并保存。文件已存在时先读取，不能在每次启动时都用初始数据覆盖它。
6. 修改一项任务的状态并保存，再运行程序，检查上次的修改是否保留。

只处理本程序正常生成的数据。JSON 内容损坏时，保留原文件和报错，交给布布一起分析；不要把读取失败当作空数据再覆盖原文件。

### 验收条件

- 第一次没有数据文件时，能够正常运行并生成文件。
- 文件中的中文可读，完成状态使用 JSON 的布尔值。
- 再次运行能够读到之前保存的状态。
- 从项目根目录和 `practice` 目录分别运行脚本，读写的都是同一个 `practice/tasks.json`。
- 能解释 Python 的 `True`/`False` 与 JSON 的 `true`/`false` 如何通过 `json` 模块转换。

### 向布布解释

- 列表里的数据和文件里的数据，分别在什么时候发生变化？
- 相对当前终端目录的路径，与相对脚本位置的路径，有什么区别？
- 为什么启动时直接写入初始数据会丢失上次的修改？

## 第七天：组合成一个命令行练习程序

**要掌握：** `while`、`break`、函数协作、综合调试。

这是七天练习的整合，不是 CampusOS 正式版本。只在 `practice/day07.py` 中完成，不搭建项目架构。

### 必做任务

1. 复用前几天已经理解的函数，写入 `day07.py`；暂时不用导入其他日期的练习脚本。
2. 程序启动时读取 `practice/tasks.json`，不存在时从空列表开始。
3. 显示以下菜单，并循环接收操作，直到用户选择退出。

```text
1. 查看全部任务
2. 查看未完成任务
3. 添加任务
4. 标记任务完成
0. 保存并退出
```

4. 选项 4 先显示全部任务及其序号，再读取序号。始终以全部任务列表编号，避免筛选后的编号与原列表不一致。
5. 复用第五天的输入检查。未知菜单选项给出提示，然后重新显示菜单。
6. 选择 `0` 时保存数据并结束程序。今天只要求正常退出时保存，直接关闭终端不保证保存本次修改。

### 验收条件

按以下顺序完成一次手动检查：

1. 将已有 `tasks.json` 改名为 `tasks.backup.json`，保留练习数据。如果备份名已存在，换一个未使用的名字，不覆盖文件。
2. 启动程序，查看全部任务和未完成任务，都应提示没有对应任务。
3. 添加两项名称不同的任务，全部任务列表应显示 2 项。
4. 把第 2 项标记为完成，未完成列表应只显示第 1 项。
5. 输入无效菜单选项、非整数任务序号和越界序号，程序仍能继续操作，任务数据不被误改。
6. 保存并退出，再次启动；两项任务及各自完成状态都应保留。

### 最后一次理解检查

请先不用 AI 修改，自己给任务列表的末尾增加一行“共 X 项，已完成 Y 项”，验证空列表和有任务时都正确。

然后用自己的话讲清楚：

- 从启动到退出，程序按什么顺序执行？
- 添加一项任务时，输入经过哪些检查，最后存到哪里？
- 哪些函数返回结果，哪些函数会修改传入的列表？
- 如果任务没保存，应该从哪里开始检查？

## 每次练习结束的记录

在 `practice/learning-log.md` 追加以下内容，几句话即可：

```markdown
### 第 N 天 / 实际日期
- 今天完成了：
- 验收结果（操作和实际输出）：
- 我能解释的知识：
- 还没弄懂的地方：
- 下次第一步：
```

七天完成后，先和布布回看代码、找出仍不熟悉的知识，再决定是否进入 CampusOS 开发。无需因为到了第七天就强行推进。
